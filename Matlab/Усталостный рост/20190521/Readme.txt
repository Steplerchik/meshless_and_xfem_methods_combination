Итак, было принято решение рассматривать нагрузку только одного знака (для 1 моды - растягивающую, для второй произвольного знака),

поскольку в этом случае К2 будет одного знака для min и max нагрузки. В результате, можно будет выбрать знак угла отклонения

направления движения трещины на основе определенного знака К2 (по умолчанию он всегда > 0). Потом определяется дельта Кeqv, который

должен быть > 0. 